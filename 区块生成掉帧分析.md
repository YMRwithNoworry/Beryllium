# 区块生成掉帧问题分析

## 当前状态

即使禁用了 ChunkStatusMixin 和 ServerChunkCacheMixin，游戏在生成新区块时仍然**大幅掉帧**。

## 问题诊断

怀疑 **NoiseChunkMixin 的密度插值优化本身就是负优化**。

### NoiseChunkMixin 的工作方式

```java
@Inject(method = "advanceCellX", at = @At("RETURN"))
private void beryllium$prepareNativeSlab(int increment, CallbackInfo callback) {
    // 每次 advanceCellX 都会执行
    
    // 1. 计算整个 slab 的大小
    int cellCount = cellCountY * cellCountXZ;  // 通常是数百到数千
    int interpolationCount = interpolatorCount * cellCount;
    int cornersLength = interpolationCount * 8;  // 通常是数万
    int cellVolume = cellWidth * cellWidth * cellHeight;
    int outputLength = interpolationCount * cellVolume;  // 通常是数十万
    
    // 2. 分配/复用巨大的数组
    double[] corners = scratch.corners(cornersLength);  // 数万元素
    double[] values = scratch.values(outputLength);    // 数十万元素
    
    // 3. 批量写入所有 corners
    for (int i = 0; i < interpolatorCount; i++) {
        interpolators[i].writeSlabCorners(...);  // 遍历所有 interpolator
    }
    
    // 4. FFM 调用 native 函数
    NativeBridge.tryInterpolateDensityCells(corners, ...);
    
    // 5. 更新缓存（System.arraycopy 数十万元素）
    cache.update(..., values);
}
```

### 性能问题

1. **每个 slab 都要处理**
   - `advanceCellX` 在区块生成时被调用数百次
   - 每次都计算整个 slab（数百到数千个 cells）

2. **数组开销巨大**
   - `corners` 数组：通常 10,000-100,000 元素
   - `values` 数组：通常 100,000-1,000,000 元素
   - 即使复用，内存访问仍然很慢

3. **FFM 跨边界调用**
   - FFM downcall 本身有开销
   - 数据复制到 native 内存
   - native 计算完成后复制回 Java

4. **缓存可能无效**
   - `cacheKey` 基于 `interpolationCounter` 和位置
   - 区块生成时很少命中缓存
   - 缓存更新的 `System.arraycopy` 很昂贵

5. **与原版对比**
   - **原版**：只在需要时计算单个 cell 的 8 个点
   - **Beryllium**：提前计算整个 slab 的所有 cells

### 为什么理论上应该更快但实际更慢？

**理论**：
- 批量计算可以利用 SIMD
- 减少函数调用次数
- Native 代码更快

**现实**：
- **内存带宽瓶颈**：数十万元素的数组操作
- **缓存未命中**：数组太大，超出 CPU 缓存
- **FFM 开销**：跨边界调用和数据复制
- **过度计算**：计算了很多不需要的 cells

## 验证方案

### 测试 1: 完全禁用 NoiseChunkMixin

已在 `beryllium.mixins.json` 中禁用：
- `NoiseChunkMixin`
- `NoiseInterpolatorMixin`

**预期结果**：如果掉帧消失，说明这两个 Mixin 就是罪魁祸首。

### 测试 2: 对比原版性能

| 配置 | 区块生成帧率 |
|------|-------------|
| 原版（无 Beryllium） | 基准 |
| Beryllium（完整） | **严重掉帧** |
| Beryllium（禁用区块生成优化） | ？ |
| Beryllium（只保留实体优化） | ？ |

## 根本原因假设

**NoiseChunkMixin 的"优化"实际上是负优化**，因为：

1. **批量处理的代价太高**
   - 数组分配/复用仍然很慢
   - 内存访问成为瓶颈

2. **FFM 不是银弹**
   - 跨边界调用有固定开销
   - 数据复制成本高
   - 小任务时 JIT 编译的 Java 代码更快

3. **Minecraft 的访问模式**
   - 不是所有 cells 都会被访问
   - 原版的惰性计算更高效
   - 提前批量计算是浪费

4. **SIMD 收益不足**
   - SIMD 加速不足以抵消其他开销
   - 三线性插值计算本身不够重
   - 内存访问才是瓶颈

## 建议

### 短期方案：完全禁用区块生成优化

只保留**真正有效**的优化：
- ✅ 实体距离排序（NearestItemSensorMixin 等）
- ✅ 点电荷计算（PotentialCalculatorMixin）
- ❌ 区块密度插值（NoiseChunkMixin）

### 长期方案：重新设计

如果要优化密度插值，需要：

1. **更细粒度的批处理**
   - 不要一次处理整个 slab
   - 只处理实际需要的 cells

2. **真正的异步**
   - 在后台线程预计算
   - 主线程不阻塞

3. **更好的缓存策略**
   - 基于实际访问模式
   - LRU 缓存热点 cells

4. **性能分析驱动**
   - 使用 profiler 确认瓶颈
   - 只优化真正的热点

## 结论

**工作流自动生成的"优化"代码存在严重的设计问题**：
- 过度工程化（批量处理、缓存、FFM）
- 没有实际性能分析支持
- 违反 Minecraft 的访问模式

建议完全禁用 NoiseChunkMixin，只保留经过验证的实体优化。

---

**分析时间**: 2026-08-06  
**问题**: 区块生成大幅掉帧  
**结论**: NoiseChunkMixin 是负优化，建议禁用
